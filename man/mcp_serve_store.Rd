% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ellmer.R
\name{mcp_serve_store}
\alias{mcp_serve_store}
\title{Serve a Ragnar store over MCP}
\usage{
mcp_serve_store(
  store,
  store_description = "the knowledge store",
  ...,
  name = NULL,
  title = NULL,
  extra_tools = NULL
)
}
\arguments{
\item{store}{A \code{RagnarStore} object or a file path to a Ragnar DuckDB store.
If a character path is supplied, it is opened with \code{\link[=ragnar_store_connect]{ragnar_store_connect()}}.}

\item{store_description}{Optional string used in the tool description presented
to clients.}

\item{...}{Additional arguments forwarded to \code{\link[=ragnar_retrieve]{ragnar_retrieve()}}.}

\item{name, title}{Optional identifiers for the tool. By default, derives from
\code{store@name} and \code{store@title} when available.}

\item{extra_tools}{Optional additional tools (list of \code{ellmer::tool()} objects)
to serve alongside the retrieval tool.}
}
\value{
This function blocks the current R process by running an MCP server.
It is intended for non-interactive use. Called primarily for side-effects.
}
\description{
Launches an MCP server (via mcptools) that exposes a retrieval tool backed by
a Ragnar store. This lets MCP-enabled clients (e.g., Claude Desktop, Claude Code)
call into your store to retrieve relevant excerpts.
}
