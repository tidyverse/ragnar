# embed functions get the defaults stored

    Code
      store@embed
    Output
      function (x) 
      ragnar::embed_openai(x)
      <environment: base>

---

    Code
      store@embed
    Output
      function (x) 
      embed_openai(x)
      <environment: base>

---

    Code
      store@embed
    Output
      function (x, ...) 
      ragnar::embed_openai(x, ...)
      <environment: base>

---

    Code
      store@embed
    Output
      function (x) 
      ragnar::embed_openai(x)
      <environment: base>

---

    Code
      store@embed
    Output
      function (x) 
      ragnar::embed_ollama(x)
      <environment: base>

